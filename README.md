# High order component with reactjs
```
mkdir foder-name,
cd folder-name,
git clone: https://github.com/AdemHuduti/HOC.git,
cd hoc,
npm install,
after installation run command npm start or yarn start
visit localhost:3000
```
